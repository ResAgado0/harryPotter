---
interface Props {
  name: string
  species: string
  gender: string
  house: string
  img: string
}

const { name, species, gender, house, img } = Astro.props;
---

<div class="buscador-result">
        <h2 class="text-2xl font-bold">{name}</h2>
        <p class="text-gray-700">Especie: {species}</p>
        <p class="text-gray-700">Género: {gender}</p>
        <p class="text-gray-700">Casa: {house}</p>
        <img src={Astro.props.img} alt={name} class="w-48 h-48 object-cover rounded-lg mt-4" />
      </div>